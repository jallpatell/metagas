# Backend (Render)
# Copy to .env on Render or your local src/api/.env for development

# Required
ARB_NODE_URL=
POL_NODE_URL=

# Optional (has a placeholder default in code, but set it in production)
ETH_NODE_URL=

# Server settings
PORT=4000
# Rolling gas history window in minutes (default 12 if unset)
GAS_HISTORY_MINUTES=12
